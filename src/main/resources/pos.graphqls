type POS {
    id: ID!
    tradingName: String!
    ownerName: String!
    document: String!
}

type Query {
    pos(id: ID!): POS
    nearby_beer(coordinate: Coordinate!): [POS]!
}

input Coordinate {
    latitude: Float!
    longitude: Float!
}